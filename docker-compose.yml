version: '3.9'

services:
    app:
        build: ./
        pull_policy: build
        restart: unless-stopped
        env_file:
            - stack.env
        ports:
            - '${PORTAINER_PORT}:3000'

networks:
    default:
        external: true
        name: nginx
