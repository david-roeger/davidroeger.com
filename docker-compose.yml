version: '3.9'

services:
    app:
        build: ./
        pull_policy: build
        restart: unless-stopped
        ports:
            - '2001:3000'
        environment:
            SUPABASE_HOST: ${SUPABASE_HOST}

networks:
    default:
        external: true
        name: nginx
